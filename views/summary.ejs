<% include header.html %>

    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <p></p>
          <a id='title' class="navbar-brand" href="#"> Hello <%- user.username %></a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-right">
            <li><a href="/summary">Summary</a></li>
            <li><a href="/userprofile">My profile</a></li>
            <li><a href="/create">Create Events</a></li>
            <li><a href="/logout">logout</a></li>
          </ul>
          <form class="navbar-form navbar-right">
            <input type="text" class="form-control" placeholder="Search...">
          </form>
        </div>
      </div>
    </nav>

    <div class="container-fluid">
      <div class="row">       
        <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
          <h1 class="page-header">Summary</h1>
          <h2 class="sub-header">All Events</h2>
          <div class="table-responsive">
            <table class="table table-striped">
              <thead>
                <tr>
                  <th>#</th>
                  <% for (var i = 0; i < users.length; i++) { %>
                    <th><%=users[i].username%></th>
                  <% } %>
                </tr>
              </thead>
              <tbody>
                <% for (var i = 0; i < events.length; i++) { %>
                  <tr>
                    <td> <%= events[i].name %> </td>
                  <% for (var j = 0; j < users.length; j++) { %>
                    <td>
                      <% for (var k = 0; k < events[i].members.length; k++) 
                            if (events[i].members[k] == users[j].username) { %>
                              <%= events[i].moneys[k] %>
                      <% } %>
                    </td>                      
                    <% } %>
                  </tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
<% include footer.html %>